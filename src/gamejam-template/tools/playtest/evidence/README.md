# C3/H4 · 2026-09-05

`passed.png` and `passed-state.txt`: real macOS keyboard playthrough of all 12 questions,
microphone capture disabled using the editor QA menu. No game-state writes by the player.
`regression.txt`: isolated production pipeline checks, not simulated keyboard events.
`network.txt`: actual UnityWebRequest against the local in-memory Code.gs fixture.
These are game-only artifacts, not desktop captures. Full recordings remain in Temp/Playtest.
